```
1. line_direction(lines)

2. line_angle(lines)

3. line_intersection(lines)

4. order_points(points, ref_point)

5. point_indexes(points, ref_points, tol=1e-6)

6. points_on_spans(points, nodes, spans, tol=1e-6)

7. normalized_distances(nodes, ref_point)

8. stations_single(paths, route)

9. stations_multiple(paths, routes)
    8. stations_single(paths, route)

10. repair_polygon(polygon_nodes, nodes_on_lines)

11. nodes_in_polygon(nodes_to_test, polygon_nodes, points)
```