```
1. read_dxf_file(file_name: str)

2. extract_points(modelspace, layer_name: str)

3. extract_lines(modelspace, layer_name: str)

4. extract_polygons(modelspace, layer_name: str)

5. extract_texts(modelspace, layer_name: str)

6. read_DXF(file_name: str)
    1. read_dxf_file(file_name: str)
    2. extract_points(modelspace, layer_name: str)
    3. extract_lines(modelspace, layer_name: str)
    4. extract_polygons(modelspace, layer_name: str)
    5. extract_texts(modelspace, layer_name: str)
```