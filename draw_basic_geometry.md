```
1. draw_init_canvas(figsize=(10, 6), dpi=100, facecolor="#f0f0f0")

2. draw_axes(ax, axes, color="black", linestyle="-.", linewidth=0.8, alpha=1)

3. draw_nodes(ax, nodes, color="black", marker="x", size=6, alpha=1)

4. draw_labels(ax, nodes, color="blue", fontsize=7, offset=(0,0))

5. draw_spans(ax, spans, nodes, color='red', linestyle="-", linewidth=2, alpha=1)

6. draw_areas(ax, areas, nodes, color="blue", linestyle="-", linewidth=2, alpha=0.5)

7. make_finalCanvasAdjustments(ax)

8. show_canvas()

9. draw_basic_geometry(...many arguments...)
```